// One project, one published artifact: life.sekejap:sekejap-ffm, the JVM binding
// over the C ABI. The e1 tree also carried `orm`, `processor` and an Android AAR
// built on a JNI glue crate; that lane is gone (see README.md).
rootProject.name = "sekejap-ffm"
