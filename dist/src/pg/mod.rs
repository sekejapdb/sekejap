//! The PostgreSQL wire protocol and the `pg` surface layered over it: the
//! startup exchange, the simple and extended query flows, and the type
//! mapping a client expects.
//!
//! NOT BUILT. This module exists so the dist layer names the surface it owes;
//! it carries no implementation, and nothing links against it.
