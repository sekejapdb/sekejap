## 0.16.4

* Version bump to stay in lockstep with the sekejap ecosystem release. No
  generator changes.

## 0.16.2

* Initial public release. `build_runner` generator for `@SekejapEntity()` models —
  emits the typed collection accessor, column references, schema descriptor, and
  JSON (de)serializers for the sekejap typed Dart API.
