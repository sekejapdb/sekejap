name: mobile_bench
description: On-device benchmark - sekejap vs popular Flutter databases.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.4
  # engines under test
  sekejap:
    path: ../../wrappers/dart
  sqflite: ^2.4.0
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  objectbox: ^5.0.0
  objectbox_flutter_libs: ^5.0.0
  hive: ^2.2.3
  realm: ^20.0.0

dev_dependencies:

flutter:
  uses-material-design: true
