[workspace]
members = ["core/kernel", "core/engine", "lang", "dist", "dist/rust", "bench"]
default-members = ["core/engine"]
resolver = "2"

[workspace.package]
version = "0.17.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[profile.release]
debug = 1
